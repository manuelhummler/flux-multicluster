apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apicurio-registry
resources:
  - ../../base/apps/apicurio-registry
generatorOptions:
  disableNameSuffixHash: true
secretGenerator:
  - name: apicurio-registry-db
    envs:
      - ../cloudnative-pg-cluster/secrets/apicurio-registry-encrypted.env
