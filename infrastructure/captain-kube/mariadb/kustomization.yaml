apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/apps/mariadb
generatorOptions:
  disableNameSuffixHash: true
secretGenerator:
  - name: mariadb-secret
    namespace: mariadb
    envs:
      - secret-encrypted.env
