apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/apps/postgres-cluster
generatorOptions:
  disableNameSuffixHash: true
secretGenerator:
  - name: postgres-operator-secret
    namespace: postgres-cluster
    envs:
      - secret-encrypted.env
