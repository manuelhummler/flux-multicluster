apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/apps/minio
secretGenerator:
  - name: minio-secret
    namespace: minio
    envs:
      - secret-encrypted.env
